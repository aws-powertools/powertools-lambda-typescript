class GraphvizVisitor {
  /**
   * TODO: write docs for GraphvizVisitor.visit()
   * TODO: finalize types for GraphvizVisitor.visit()
   *
   * @param _node
   * @returns
   */
  public visit(_node: unknown): string {
    return '';
  }
}

export { GraphvizVisitor };
